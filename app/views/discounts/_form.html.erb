<%= form_with(model: discount, local: true) do |form| %>
  <% if discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discount.errors.count, "error") %> prohibited this discount from being saved:</h2>

      <ul>
        <% discount.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :discount_percentage %>
    <%= form.text_field :discount_percentage %>
  </div>

  <div class="field">
    <%= form.label :starts_at %>
    <%= form.date_select :starts_at %>
  </div>

  <div class="field">
    <%= form.label :kind_of_plan %>
    <%= form.number_field :kind_of_plan %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
